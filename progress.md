Original prompt: Implement Reliable Arcade Factory: preserve folders and all 33 Plan/Build/Review goals; complete and validate the harness and 1000 distinct games through it.

Latest steering: prioritize seeded list matching and compatible combinations; expand reusable catalog data before new mechanics; use models for bounded residual gaps. Applied to the execution contract and all 33 goal interpretations. Six contract consistency checks passed; no capabilities made eligible. Next G01 action: map existing pilot behaviors to catalog options and distinguish mapping gaps from missing engine behavior before planning more implementation.

Current mission activation: implement the complete attached 33-goal Reliable Arcade Factory plan and push validated increments to origin main.

2026-09-13 contract-5 checkpoint: updated all 33 existing JSON goals with layer and replay obligations; added four catalog points (27 total, 55 options/capabilities), mandatory two-run replay evidence, recovery thresholds, and qualification/review policy. New capabilities remain missing; no acceptance granted. Added four-file contract identity and stale version/catalog/reference rejection. G01 readiness was revalidated for continued audit only; G01 remains running, G02-G33 planned, accepted 0/1000.

Validated this increment: 14 independent contract/negative probes, existing 41 tests plus build/browser boundary, npm package dry-run (103 files; no run/model/capture leakage). Package stdout included build logging before JSON; parsed the JSON array after that prefix. Local LM Studio endpoint responds with the required planner/writer aliases; inference/vision were not exercised in this increment.

Next action: finish G01's nine calibration/design records (including replay) from actual pilot evidence. Resolve three complete pilot composition contracts; then complete G01 with phase evidence and advance to G02. G02 must integrate executable catalog composition, generic repair, timely qualification versus independent review, and shared-cause admission recovery. Do not count existing fixed pilots as generic compiler proof.



Current implementation status: foundation increment, not full plan completion. Preserved the 33-goal order and root structure. Added category contracts, canonical hashing, a 25-minute deadline, guarded leases/queue/publication, bounded model-response correction, dependency-aware improvement, catalog validation/intermediate resolution, a paged arcade, and three shared pilot families. No generated game code was hand-edited.

Verified: existing 41 tests/build/browser boundary; 47 pilot checks; 12 controller checks; 10 UI checks; the supplied web-game client on all three pilots; immutable artifact/runtime hashes; synthetic 1000-row library pagination/search. The initial npm run hit macOS symlinked temp-path checks; TMPDIR=/private/tmp passed. Default graphics-backend initialization failed, so there is no target-device FPS or concurrent-generation proof.

Pilot outcomes: factory-courier-05 (Energy Courier), factory-conduit-01 (Valve Harmony), factory-rally-01 (Rally) pass development preview checks but remain NEEDS_REVIEW. Four failed courier probes are retained: two renderer-check failures, one truncated writer response and one negative visual review. Shared fixes enlarged labels, supplied focused image evidence, separated active-scene rendering from end-state geometry, and adapted truncated model responses. Retry lineage on the final courier preserves its original deadline. Foundation probes never count as production slots.

Next required work: finish the catalog-to-domain compiler and integrate pilots with the full feature improvement controller; resolve the nine G01 calibration records from real evidence. Then independently review concept contribution, visual/interaction novelty, presentation and device performance before any 100-game batch. Accepted factory games: 0/1000. No goal is marked complete.

Evidence and checkpoint: NexusArcade-Experiments/campaigns/reliable-arcade-factory/goals/G01/execution-packet.json and evidence-manifest.json. Source documentation: NexusArcade-Harness/README.md. Local arcade remains on 127.0.0.1:4318.

Historical replacement-campaign notes follow; their earlier PASS rubric is not factory acceptance.

NexusArcade replacement campaign

Current request: replace the old cohort with 50 distinct Three.js/NexusEngine compositions and an arcade UI. Removed exactly final50-001 through final50-050; other historical trials remain under an explicit legacy filter.

Pinned the full-source import closure from NexusEngine a74e8689d1a71c0b42236c009f0f4c46e9b89387. Added bounded abstract-concept expansion, composition DAG validation, installed upstream locomotion/affordance/cargo/transfer/progression kits, a local optional composition adapter kit, Three.js presentation, immutable shared runtime snapshots, and an arcade card UI. The replacement implementation and campaign are complete; see NexusArcade-Harness/VALIDATION.md.

Spatial pilot results: spatial-pilot5 passed, varied pilots 4/4 passed, objective/bridge-closure pilots 3/3 passed, final two-view/five-minute pilot passed. Used supplied web-game client and inspected screenshots. Source-only negative checks proved engine prerequisite rejection, physical edge blocking and full session reset.

The initial arcade50 campaign was stopped after 4 passes and 1 cancellation to strengthen diversity comparison: unused receiver settings and descriptive goal names must not count as behavioral uniqueness. Its evidence remains historical. Current measured campaign: arcade50-final, seeds 94000–94049, session log /tmp/nexus-arcade50-final.log. Runtime source is frozen during the campaign. UI defaults to arcade50-final and interactive game-* runs; other development runs require Advanced history.

A separate real-time five-minute idle test passed on arcade50-001 (same gameplay implementation; only later diversity comparison differs). Performance sample while multiple software-WebGL browsers were active: median 41.4 ms, p95 50 ms; not hardware/cabinet proof. Do not claim AAA fidelity or hardware 60 FPS.

Completed: 50 accepted distinct Three.js compositions from 54 attempts, including four fresh replacements. Final CLI verify checked all 50 artifacts, compositions, two screenshots per game and shared runtime hashes. All accepted games use one frozen runtime revision. Mean accepted generation 67.047 seconds; p95 144.884; max 224.542. Mean reported output tokens 80.64; input/image counts reported separately.

Four failures retained: two visual request timeouts (one host wall-clock interruption), one browser load timeout, one negative visual verdict. None were relabeled. Main UI shows exactly the accepted collection; failed and historical runs are under Advanced. Temporary process-scoped sleep inhibition covered the remaining generation.

Validation: existing 41 tests/build/browser-boundary, 1000 composition graph checks, final supplied-client screenshot inspection, UI Play/Back/history/mobile width and final integrity audit passed. Human enjoyment, AAA fidelity and cabinet/hardware performance remain unverified. Generated games were never manually edited. No commit, push or deployment.

Current implementation: contracts/queue/deadline/locks foundation underway. Existing games remain immutable; no new factory acceptance claimed.
